# A ticket title describing the ban is not the ban being broken.
#
# Same class as 04 and worth its own case: the earlier defect was found twice
# in ci_cost_guard's history, once on a ticket description and once on a test
# fixture. Neither spent a minute of anybody's Actions quota.
COMMAND='ticket new --type chore --title "document why gh workflow run is refused"'
