# The rule it exists for. Patrick's IDE resolves markdown links against the
# workspace root (claude-env), so a relative link to another repo 404s.
REPLY='See [queue.py](dashboard/queue.py) for the change.'
