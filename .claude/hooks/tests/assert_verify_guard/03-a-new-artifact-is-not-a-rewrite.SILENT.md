# The same path, but the file does not exist yet. Creating a record is not
# restating claims in one, and the hook draws that line deliberately.
TOOL_NAME="Write"
FILE_PATH="sessionState.md"
CONTENT="## Status
first write"
